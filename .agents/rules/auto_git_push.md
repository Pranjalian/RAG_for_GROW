# Automatic Git Push

When you make changes to files in this project:
1. Always run `git add` for the modified files.
2. Run `git commit -m "[Brief description of changes]"`
3. Run `git push origin main` (or the current branch) automatically.
4. Do NOT ask the user for permission before committing and pushing. You should do this autonomously after verifying your changes.
